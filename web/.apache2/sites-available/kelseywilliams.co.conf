<VirtualHost *:443>
	ServerName "kelseywilliams.co"
        ServerAdmin admin@kelseywilliams
        DocumentRoot /var/www/html

	SSLEngine on
	SSLCertificateFile /.apache2/certs/kelseywilliams.co.crt
	SSLCertificateKeyFile /.apache2/certs/kelseywilliams.co.key
	SSLCertificateChainFile /.apache2/certs/kelseywilliams.co.ca

        ErrorLog ${APACHE_LOG_DIR}/error.log
        CustomLog ${APACHE_LOG_DIR}/access.log combined
        Options Includes

        <Directory "/var/www/html/">
                AllowOverride all
                
                # Rewrite for routing of all requests through REST controller
                #RewriteEngine On
                # To internally forward /dir/foo to /dir/foo.php
                #RewriteCond %{REQUEST_FILENAME} !-d
                #RewriteCond %{REQUEST_FILENAME}.php -f
                #RewriteRule ^(.*?)/?$ $1.php [L]
        </Directory>
</VirtualHost>
